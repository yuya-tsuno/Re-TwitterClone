<h3>以下の内容で、送信する。</h3>

<%= form_with(model: @tweet, local: true) do |form| %>
  <%= form.hidden_field :content %>

  <p>ツイート本文:<%= @tweet.content %></p>
  <%= form.submit  "登録する" %>

  <%= form.submit "戻る", name: 'back' %>

<% end %>

